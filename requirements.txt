# MQTT-SDN Priority Simulation - Python Dependencies
# Install with: pip install -r requirements.txt

# MQTT Client
paho-mqtt>=1.6.0

# SDN Controller (Ryu)
ryu>=4.34

# Excel export (optional, for generate_summary_manual_v2.py)
openpyxl>=3.0.0

# Ryu dependencies (usually auto-installed with ryu)
eventlet>=0.30.0
msgpack>=1.0.0
netaddr>=0.8.0
oslo.config>=8.0.0
routes>=2.5.0
tinyrpc>=1.0.0
webob>=1.8.0

# Note: The following are system packages, install via apt:
# - mininet (sudo apt install mininet)
# - openvswitch-switch (sudo apt install openvswitch-switch)
# - mosquitto (sudo apt install mosquitto mosquitto-clients)
